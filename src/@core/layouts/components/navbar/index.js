// ** React Imports
import { Fragment } from 'react'

// ** Custom Components


const ThemeNavbar = props => {
  // ** Props

  return (
    <nav>
      nav bar
    </nav>
  )
}

export default ThemeNavbar
